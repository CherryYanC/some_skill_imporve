/** 
 * 浅拷贝
 */

const _shallowClone = function(target) {
    // 基本数据类型直接返回
    if (typeof target === 'object' && target !== null) {
        const constructor = target.constructor
        // 如果构造体为以下几种类型直接返回
    } else {
        return target
    }
}
